{
    "version": "2.0.0",
    "tasks": [
        {
            "options": {
                "cwd": "${workspaceFolder}/web-client"
            },
            "label": "vuejs: start",
            "type": "npm",
            "script": "serve",
            "isBackground": true
        }
    ]
}